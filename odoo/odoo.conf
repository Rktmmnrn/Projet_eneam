[options]
; === SECURITE ===
admin_passwd = odoo
list_db = True

; === BASE DE DONNEES ===
; Nom du service PostgreSQL dans Docker
db_host = postgres-odoo
db_port = 5432
db_user = odoo
db_password = odoo
; db_name = databaseOdoo
; Commenté : mode wizard pour créer une DB depuis l'interface

; === SMTP CONFIGURATION ===
email_from = contact@eneam.mg
smtp_server = mailhog      # ← Nom du service Docker
smtp_port = 1025           # ← Port MailHog SMTP
smtp_user = False
smtp_password = False
smtp_encryption = none

; === CHEMINS ===
addons_path = /mnt/extra-addons,/usr/lib/python3/dist-packages/odoo/addons
data_dir = /var/lib/odoo

; === RESEAU ===
; Interface d'écoute (0.0.0.0 = toutes)
http_interface = 0.0.0.0
http_port = 8069
; Pas de proxy devant
proxy_mode = False

; === OPTIONS ===
without_demo = True                # Pas de données de démo